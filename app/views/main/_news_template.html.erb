<script type="text/x-jquery-tmpl" id="dataTemplate">
	{{if header}}
		<h3 class="header">${header}</h3>
	{{/if}}
	{{if image}}
		<%= link_to image_tag('${image}'), :class => "leftimg" %>
	{{/if}}
	{{each text}}
		<p>${$value}</p>
	{{/each}}
</script>
